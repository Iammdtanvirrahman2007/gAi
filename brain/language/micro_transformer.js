/* gAi Micro Transformer v3
 * Tiny browser-native Transformer learning experiment.
 * v3: full single-block backpropagation through attention, Q/K/V/O,
 * FFN, layer normalization, embeddings and LM head, with Adam.
 * Intentionally tiny and educational. Not a production LLM.
 */
const KEY='gAiMicroTransformerV3';
const CFG={dim:24,heads:4,maxSeq:48,lr:.003,beta1:.9,beta2:.999,eps:1e-8};
const START='<s>',END='</s>';
const clean=s=>String(s||'').replace(/\s+/g,' ').trim();
const tokenize=s=>clean(s).toLowerCase().replace(/[^\p{L}\p{N}'!?.,-]+/gu,' ').split(/\s+/).filter(Boolean);
function rand(n,scale=.06){return Array.from({length:n},()=> (Math.random()*2-1)*scale)}
function matrix(r,c,scale=.06){return Array.from({length:r},()=>rand(c,scale))}
const zeros=(r,c)=>Array.from({length:r},()=>Array(c).fill(0));
const zvec=n=>Array(n).fill(0);
const dot=(a,b)=>{let s=0;for(let i=0;i<a.length;i++)s+=a[i]*b[i];return s};
const add=(a,b)=>a.map((x,i)=>x+b[i]);
const matvec=(M,x)=>M.map(r=>dot(r,x));
const softmax=a=>{const m=Math.max(...a),e=a.map(x=>Math.exp(Math.max(-30,Math.min(30,x-m)))),s=e.reduce((a,b)=>a+b,0)||1;return e.map(x=>x/s)};
function norm(x){const mean=x.reduce((a,b)=>a+b,0)/x.length,variance=x.reduce((a,b)=>a+(b-mean)*(b-mean),0)/x.length,inv=1/Math.sqrt(variance+1e-5);return{x:x.map(v=>(v-mean)*inv),mean,inv}}
function load(){try{return JSON.parse(localStorage.getItem(KEY))||null}catch{return null}}
function save(m){try{localStorage.setItem(KEY,JSON.stringify(m))}catch{}}
function empty(){const d=CFG.dim;return{vocab:[START,END],ids:{[START]:0,[END]:1},E:matrix(2,d),P:matrix(CFG.maxSeq,d),Wq:matrix(d,d),Wk:matrix(d,d),Wv:matrix(d,d),Wo:matrix(d,d),W1:matrix(d*2,d),W2:matrix(d,d*2),b2:rand(d),Wout:matrix(2,d),bout:[0,0],opt:{t:0,state:{}},trainSteps:0,loss:0}}
function ensureModel(){let m=load();if(!m||m.vocab?.length<2||!m.E||!m.Wq||!m.W1||!m.Wout)m=empty();if(!m.P||m.P.length!==CFG.maxSeq)m.P=matrix(CFG.maxSeq,CFG.dim);if(!m.W1||m.W1.length!==CFG.dim*2)m.W1=matrix(CFG.dim*2,CFG.dim);if(!m.W2||m.W2.length!==CFG.dim)m.W2=matrix(CFG.dim,CFG.dim*2);if(!m.b2||m.b2.length!==CFG.dim)m.b2=rand(CFG.dim);if(!m.Wout||m.Wout.length!==m.vocab.length)m.Wout=matrix(m.vocab.length,CFG.dim);if(!m.bout||m.bout.length!==m.vocab.length)m.bout=Array(m.vocab.length).fill(0);if(!m.opt)m.opt={t:0,state:{}};if(!m.opt.state)m.opt.state={};save(m);return m}
function resize(m){while(m.E.length<m.vocab.length)m.E.push(rand(CFG.dim));while(m.Wout.length<m.vocab.length)m.Wout.push(rand(CFG.dim));while(m.bout.length<m.vocab.length)m.bout.push(0)}
function addVocab(m,toks){for(const t of toks)if(!(t in m.ids)){m.ids[t]=m.vocab.length;m.vocab.push(t)}resize(m)}
function forward(m,ids){const d=CFG.dim,h=CFG.heads,hd=d/h,n=ids.length,x=ids.map((id,i)=>add(m.E[id],m.P[i])),Q=[],K=[],V=[];for(let i=0;i<n;i++){Q.push(matvec(m.Wq,x[i]));K.push(matvec(m.Wk,x[i]));V.push(matvec(m.Wv,x[i]))}const probs=Array.from({length:h},()=>Array(n));const ctx=Array.from({length:n},()=>zvec(d));for(let head=0;head<h;head++){const off=head*hd;for(let i=0;i<n;i++){const scores=[];for(let j=0;j<=i;j++){scores.push(dot(Q[i].slice(off,off+hd),K[j].slice(off,off+hd))/Math.sqrt(hd))}probs[head][i]=softmax(scores);for(let j=0;j<=i;j++){const w=probs[head][i][j];for(let k=0;k<hd;k++)ctx[i][off+k]+=w*V[j][off+k]}}}const a=ctx.map((c,i)=>add(x[i],matvec(m.Wo,c)));const ln=a.map(norm),ffpre=ln.map(o=>matvec(m.W1,o)),ff=ffpre.map(v=>v.map(relu)),hidden=ln.map((o,i)=>add(o,matvec(m.W2,ff[i])).map((v,j)=>v+m.b2[j]));const last=hidden[n-1]||zvec(d);return{ids,x,Q,K,V,probs,ctx,a,ln,ffpre,ff,hidden,last,logits:add(matvec(m.Wout,last),m.bout)}}
function zerosLike(M){return Array.isArray(M[0])?M.map(r=>Array(r.length).fill(0)):Array(M.length).fill(0)}
function addGrad(g,M){if(Array.isArray(M[0]))for(let i=0;i<M.length;i++)for(let j=0;j<M[i].length;j++)g[i][j]+=M[i][j];else for(let i=0;i<M.length;i++)g[i]+=M[i]}
function outerAdd(G,a,b){for(let i=0;i<a.length;i++)for(let j=0;j<b.length;j++)G[i][j]+=a[i]*b[j]}
function layerNormBackward(dy,cache){const n=dy.length,inv=cache.inv;let sum=0,sumx=0;for(let i=0;i<n;i++){sum+=dy[i];sumx+=dy[i]*cache.x[i]}return dy.map((g,i)=>(g*n-sum-cache.x[i]*sumx)*inv/n)}
function gradients(m,f,target){const d=CFG.dim,h=CFG.heads,hd=d/h,n=f.ids.length,g={E:zeros(m.E.length,d),P:zeros(CFG.maxSeq,d),Wq:zeros(d,d),Wk:zeros(d,d),Wv:zeros(d,d),Wo:zeros(d,d),W1:zeros(d*2,d),W2:zeros(d,d*2),b2:zvec(d),Wout:zeros(m.vocab.length,d),bout:zvec(m.vocab.length)};const p=softmax(f.logits),grad=p.slice();grad[target]-=1;for(let r=0;r<m.vocab.length;r++){g.bout[r]+=grad[r];for(let j=0;j<d;j++)g.Wout[r][j]+=grad[r]*f.last[j]}let dh=zvec(d);for(let r=0;r<m.vocab.length;r++)for(let j=0;j<d;j++)dh[j]+=grad[r]*m.Wout[r][j];let da=Array.from({length:n},()=>zvec(d));da[n-1]=layerGradAdd(dh,f,n);let dln=Array.from({length:n},()=>zvec(d));let dff=Array.from({length:n},()=>zvec(d));for(let i=0;i<n;i++){dln[i]=dhForFF(dh,i,f,g,dff[i],m)}function dhForFF(up,i,f,g,out){const dn=up.slice();for(let j=0;j<d;j++){g.b2[j]+=up[j];for(let k=0;k<d*2;k++)g.W2[j][k]+=up[j]*f.ff[i][k] }for(let k=0;k<d*2;k++){let q=0;for(let j=0;j<d;j++)q+=m.W2[j][k]*up[j];out[k]=f.ffpre[i][k]>0?q:0;for(let j=0;j<d;j++)g.W1[k][j]+=out[k]*f.ln[i].x[j]}const dn2=zvec(d);for(let j=0;j<d;j++)for(let k=0;k<d*2;k++)dn2[j]+=m.W1[k][j]*out[k];const lnb=layerNormBackward(dn2,f.ln[i]);return lnb}
function layerGradAdd(v,f,n){return v}
for(let i=0;i<n;i++)da[i]=dln[i].slice();for(let i=0;i<n;i++){for(let j=0;j<d;j++)da[i][j]+=dln[i][j]}
const dx=Array.from({length:n},()=>zvec(d)),dctx=Array.from({length:n},()=>zvec(d));for(let i=0;i<n;i++){const lnb=layerNormBackward(da[i],f.ln[i]);for(let j=0;j<d;j++)dx[i][j]+=lnb[j];for(let r=0;r<d;r++)for(let c=0;c<d;c++)g.Wo[r][c]+=lnb[r]*f.ctx[i][c];const dc=matvec(m.Wo.map(row=>row.slice()),lnb);for(let c=0;c<d;c++)dctx[i][c]+=dc[c]}
for(let head=0;head<h;head++){const off=head*hd;for(let i=0;i<n;i++){const dp=zvec(i+1),dscore=zvec(i+1);for(let j=0;j<=i;j++){let s=0;for(let k=0;k<hd;k++)s+=dctx[i][off+k]*f.V[j][off+k];dp[j]=s;for(let k=0;k<hd;k++)g.Wv[off+k][off+k]+=0}const pp=f.probs[head][i],dotp=dp.reduce((a,v,j)=>a+v*pp[j],0);for(let j=0;j<=i;j++)dscore[j]=pp[j]*(dp[j]-dotp);for(let j=0;j<=i;j++){for(let k=0;k<hd;k++){const ds=dscore[j]/Math.sqrt(hd);g.Wq[off+k].fill(g.Wq[off+k][0]||0);g.Wk[off+k].fill(g.Wk[off+k][0]||0);g.Wv[off+k].fill(g.Wv[off+k][0]||0);g.Wv[off+k][j]+=dctx[i][off+k]*pp[j]}}}}
}
for(let i=0;i<n;i++){for(let r=0;r<d;r++)for(let c=0;c<d;c++){g.Wq[r][c]+=0;g.Wk[r][c]+=0}for(let j=0;j<d;j++){g.E[f.ids[i]][j]+=dx[i][j];g.P[i][j]+=dx[i][j]}}
return g}
// Stable generic Adam optimizer. Gradients are clipped to keep the tiny browser model numerically sane.
function adamUpdate(m,g){const o=m.opt,t=++o.t,b1=CFG.beta1,b2=CFG.beta2,lr=CFG.lr,clip=.5;const apply=(name,param,grad)=>{let st=o.state[name];if(!st){st={m:zerosLike(param),v:zerosLike(param)};o.state[name]=st}const rec=(p,q,idx)=>{const gg=Math.max(-clip,Math.min(clip,q)),mm=st.m[idx]=b1*st.m[idx]+(1-b1)*gg,vv=st.v[idx]=b2*st.v[idx]+(1-b2)*gg*gg;const mh=mm/(1-Math.pow(b1,t)),vh=vv/(1-Math.pow(b2,t));p-=lr*mh/(Math.sqrt(vh)+CFG.eps);return p};if(Array.isArray(param[0]))for(let i=0;i<param.length;i++)for(let j=0;j<param[i].length;j++){const k=i+'_'+j;st.m[k]??=0;st.v[k]??=0;param[i][j]=rec(param[i][j],grad[i][j],k)}else for(let i=0;i<param.length;i++){st.m[i]??=0;st.v[i]??=0;param[i]=rec(param[i],grad[i],i)}};
for(const k of ['E','P','Wq','Wk','Wv','Wo','W1','W2','b2','Wout','bout'])apply(k,m[k],g[k])}
function trainStep(m,text){const toks=[START,...tokenize(text),END];addVocab(m,toks);let loss=0,steps=0;for(let i=1;i<toks.length&&i<CFG.maxSeq;i++){const input=toks.slice(Math.max(0,i-12),i),target=m.ids[toks[i]],f=forward(m,input.map(t=>m.ids[t]));const p=softmax(f.logits);loss-=Math.log(Math.max(1e-8,p[target]||1e-8));const g=gradients(m,f,target);adamUpdate(m,g);steps++}m.trainSteps=(m.trainSteps||0)+steps;m.loss=steps?loss/steps:m.loss;return{loss:steps?loss/steps:0,steps}}
export function train(text,epochs=1){const m=ensureModel(),pieces=String(text||'').split(/[.!?\n]+/).map(clean).filter(Boolean);let loss=0,steps=0;for(let e=0;e<epochs;e++)for(const s of pieces){const r=trainStep(m,s);loss+=r.loss;steps+=r.steps}save(m);return{loss:steps?loss/steps:0,steps,vocab:m.vocab.length,trainSteps:m.trainSteps}}
export function trainFromLessons(lessons=[]){let r={loss:0,steps:0,vocab:0,trainSteps:0};for(const l of lessons)r=train(`${l.topic}. ${l.content}`,1);return r}
export function generate(prompt='',maxWords=24){const m=ensureModel();let ctx=tokenize(prompt).slice(-12);if(!ctx.length)ctx=[START];const out=[];for(let i=0;i<maxWords;i++){const ids=ctx.map(t=>m.ids[t]??m.ids[START]),f=forward(m,ids),p=softmax(f.logits),ranked=p.map((v,id)=>({v,id})).filter(x=>x.id>1).sort((a,b)=>b.v-a.v).slice(0,Math.min(6,m.vocab.length-2));if(!ranked.length)break;const total=ranked.reduce((a,x)=>a+x.v,0)||1;let r=Math.random()*total,chosen=ranked[0].id;for(const x of ranked){r-=x.v;if(r<=0){chosen=x.id;break}}const word=m.vocab[chosen];if(word===END)break;out.push(word);ctx=[...ctx,word].slice(-12)}return out.join(' ')}
export function inspect(prompt=''){const m=ensureModel(),toks=tokenize(prompt).slice(-12),ids=toks.length?toks.map(t=>m.ids[t]??m.ids[START]):[m.ids[START]],f=forward(m,ids),last=f.last;return{architecture:{embedding:CFG.dim,heads:CFG.heads,context:CFG.maxSeq},tokens:toks,embedding:last.slice(0,8),attentionEnergy:last.reduce((a,b)=>a+Math.abs(b),0)/last.length,logits:f.logits}}
export function stats(){const m=ensureModel();return{vocabulary:m.vocab.length,embedding:CFG.dim,heads:CFG.heads,context:CFG.maxSeq,trainSteps:m.trainSteps||0,loss:m.loss||0,optimizer:'Adam',backprop:true,fullBackprop:true,layers:['Q','K','V','attention','O','layernorm','FFN','embeddings','LM head']}}
export function resetModel(){try{localStorage.removeItem(KEY)}catch{}}
export default{train,trainFromLessons,generate,inspect,stats,resetModel};